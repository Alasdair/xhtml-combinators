{-# LANGUAGE NoImplicitPrelude #-}
module Text.XHtmlCombinators.Clash
    ( module Text.XHtmlCombinators
    , head, div, span, map 
    ) where

import Text.XHtmlCombinators

head x = head_ x

div x = div_ x

span x = span_ x

map x = map_ x